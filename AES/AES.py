#block_hex = input("Insert 16hex symbols \n")
#block = bytes.fromhex(block_hex)
#print(block)

key = input("Insert hex key 16bytes\n")
secret = bytes.fromhex(key)

#state = bytes(p^k for p,k in zip(block,secret)) #Тут создаю массив стейт который хорит сразу инишл ки и наш блок 128бит




